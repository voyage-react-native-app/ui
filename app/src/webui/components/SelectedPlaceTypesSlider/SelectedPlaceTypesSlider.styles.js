import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    sliderContainer: {
        flexDirection: 'row',
        minWidth: '100%',
        height: 60,
        marginTop: 25,
        marginBottom: 12,
        alignItems: 'center'
    }
});
