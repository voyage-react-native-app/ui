import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    bigButtonContainer: {
        borderRadius: 10,
        width: '100%',
        height: 60,
        justifyContent: 'center',
        alignItems: 'center'
    },
    buttonTextWrapper: {
        width: '100%',
        height: '100%',
        justifyContent: 'center',
        alignItems: 'center'
    },
    buttonText: {
        fontSize: 15,
        fontWeight: '700'
    }
});
