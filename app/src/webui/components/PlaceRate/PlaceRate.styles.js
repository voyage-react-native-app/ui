import { StyleSheet } from 'react-native';

import { colors } from '../../modules/styles/colors.styles';

export const styles = ({
    placeRateWrapper: {
        flexDirection: 'row',
        alignItems: 'center'
    },
    placeRate: {
        color: colors.yellow,
        fontSize: 16,
        marginLeft: 2
    }
});
