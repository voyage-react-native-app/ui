import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    backArrowContainer: {
        width: 50,
        height: 50,
        borderRadius: 25,
        position: 'absolute'
    },
    backArrowWrapper: {
        width: '100%',
        height: '100%',
        justifyContent: 'center',
        alignItems: 'center'
    }
});
