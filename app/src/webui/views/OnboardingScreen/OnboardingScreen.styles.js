import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    paginatorWrapper: {
        flex: 1,
        paddingTop: 20,
        width: '100%',
        alignItems: 'center'
    },
    footerWrapper: {
        flexDirection: 'row'
    }
});
